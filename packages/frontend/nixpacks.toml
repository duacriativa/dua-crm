[phases.setup]
cmds = ["echo 'cache-bust-v3-2026-06-02'"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["rm -rf .next/cache node_modules/.cache", "npm run build"]
