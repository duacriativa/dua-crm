[phases.install]
cmds = ["npm install --legacy-peer-deps --include=dev"]

[phases.build]
cmds = ["npx prisma generate"]

[start]
cmd = "npx prisma db push --accept-data-loss && npx ts-node prisma/seed.ts && npx ts-node --transpile-only src/main.ts"
